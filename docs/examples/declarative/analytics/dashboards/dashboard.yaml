_defaults:
  kongctl:
    namespace: analytics

analytics:
  dashboards:
    - ref: quick-summary-dashboard
      name: Quick Summary Dashboard
      definition: !file ./definitions/quick-summary.json
      labels:
        team: platform
        purpose: observability
