event_gateways:
  - ref: getting-started-event-gateway
    name: "My First Event Gateway"
    description: "My first event gateway control plane for managing event-driven architectures"
    kongctl:
      namespace: "default"
    listeners:
      - ref: default-event-gateway-listener
        name: default-event-gateway-listener
        description: "Default Event Gateway Listener"
        ports:
          - "9093-9095"
        addresses:
          - localhost