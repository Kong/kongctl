# Kong Identity directories.

identity:
  directories:
    - ref: directory-1
      name: example-directory-1
      description: Directory 1 identities available to all control planes
      allow_all_control_planes: true
      ttl_secs: 600
      negative_ttl_secs: 300
