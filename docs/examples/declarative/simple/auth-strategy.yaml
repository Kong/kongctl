# Simple Authentication Strategy Example
# Minimal configuration for an auth strategy

application_auth_strategies:
  - ref: simple-auth
    name: my-api-key-auth
    display_name: API Key Authentication
    strategy_type: key_auth
    configs:
      key_auth:
        key_names:
          - X-API-Key
    labels:
      foo: abc
