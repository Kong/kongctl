# Test data with API having multiple versions (should fail validation)
apis:
  - ref: multi-version-api
    name: "Multi Version API"
    description: "An API with multiple versions"
    
    versions:
      - ref: api-v1
        version: "1.0.0"
      
      - ref: api-v2
        version: "2.0.0"
