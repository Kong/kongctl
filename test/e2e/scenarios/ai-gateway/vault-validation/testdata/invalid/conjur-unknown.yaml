ai_gateways:
  - ref: gateway
    name: gateway
    display_name: Gateway
ai_gateway_vaults:
  - ref: vault
    ai_gateway: gateway
    name: vault
    type: conjur
    config:
      account: account
      login: workload
      endpoint_url: https://conjur.example.net
      unknown_field: value
