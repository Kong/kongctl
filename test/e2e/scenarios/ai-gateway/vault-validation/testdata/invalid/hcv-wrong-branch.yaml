ai_gateways:
  - ref: gateway
    name: gateway
    display_name: Gateway
ai_gateway_vaults:
  - ref: vault
    ai_gateway: gateway
    name: vault
    type: hcv
    config:
      auth_method: token
      host: vault.example.net
      port: 8200
      nonce: value
