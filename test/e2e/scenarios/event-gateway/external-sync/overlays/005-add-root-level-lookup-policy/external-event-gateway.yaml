event_gateway_virtual_cluster_cluster_policies:
  - ref: "{{ .vars.lookup_cluster_policy_ref }}"
    name: "{{ .vars.lookup_cluster_policy_name }}"
    description: "{{ .vars.lookup_cluster_policy_description }}"
    event_gateway: !lookup {name: external-sync-event-gateway}
    virtual_cluster: !lookup {name: external-sync-virtual-cluster}
    type: acls
    enabled: true
    config:
      rules:
        - action: deny
          operations:
            - name: describe_configs
          resource_names:
            - match: "*"
          resource_type: transactional_id
