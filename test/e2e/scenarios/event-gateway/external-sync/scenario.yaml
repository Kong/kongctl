baseInputsPath: ./testdata

env:
  KONGCTL_LOG_LEVEL: info

vars:
  egw_name: external-sync-event-gateway
  backend_cluster_name: external-sync-backend-cluster
  virtual_cluster_name: external-sync-virtual-cluster
  lookup_cluster_policy_ref: external-sync-lookup-cluster-policy
  lookup_cluster_policy_name: external-sync-lookup-cluster-policy
  lookup_cluster_policy_description: Cluster policy added via !lookup reference
  cluster_policy_ref: external-sync-cluster-policy
  cluster_policy_name: external-sync-cluster-policy
  produce_policy_ref: external-sync-produce-policy
  produce_policy_name: external-sync-produce-policy
  produce_policy_description: External sync produce policy
  consume_policy_ref: external-sync-consume-policy
  consume_policy_name: external-sync-consume-policy
  consume_policy_description: External sync consume policy

defaults:
  mask:
    dropKeys:
      - id
      - created_at
      - updated_at

steps:
  - name: 000-reset-org
    skipInputs: true
    commands:
      - resetOrg: true

  - name: 001-bootstrap-out-of-band-event-gateway
    skipInputs: true
    commands:
      - name: 000-create-event-gateway
        create:
          resource: event_gateway
          payload:
            inline:
              name: "{{ .vars.egw_name }}"
              description: "External sync event gateway"
          recordVar:
            name: egw_id
            responsePath: id

      - name: 001-create-backend-cluster
        create:
          resource: event-gateway-backend-cluster
          endpointParams:
            gatewayId: "{{ .vars.egw_id }}"
          payload:
            inline:
              name: "{{ .vars.backend_cluster_name }}"
              description: "External sync backend cluster"
              authentication:
                type: anonymous
              insecure_allow_anonymous_virtual_cluster_auth: true
              bootstrap_servers:
                - "external-sync-backend-1.example.com:9092"
                - "external-sync-backend-2.example.com:9092"
              tls:
                enabled: true
                insecure_skip_verify: false
                tls_versions:
                  - tls12
                  - tls13
          recordVar:
            name: backend_cluster_id
            responsePath: id

      - name: 002-create-virtual-cluster
        create:
          resource: event-gateway-virtual-cluster
          endpointParams:
            gatewayId: "{{ .vars.egw_id }}"
          payload:
            inline:
              name: "{{ .vars.virtual_cluster_name }}"
              description: "External sync virtual cluster"
              destination:
                id: "{{ .vars.backend_cluster_id }}"
              authentication:
                - type: anonymous
              acl_mode: enforce_on_gateway
              dns_label: external-sync-vc
          recordVar:
            name: virtual_cluster_id
            responsePath: id

  - name: 002-sync-external-reference
    commands:
      - name: 000-sync-dry-run
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --dry-run
        assertions:
          - select: plan.metadata
            expect:
              fields:
                mode: sync
          - select: plan.summary
            expect:
              fields:
                total_changes: 0
          - select: plan.changes[?resource_type=='event_gateway_backend_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0

  - name: 003-sync-with-lookup-root-level-cluster-policy
    inputOverlayDirs:
      - overlays/005-add-root-level-lookup-policy
    commands:
      - name: 000-sync-add-lookup-cluster-policy
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 1
                by_action.CREATE: 1
          - select: >-
              plan.changes[?resource_type=='event_gateway_virtual_cluster_cluster_policy' &&
              resource_ref=='{{ .vars.lookup_cluster_policy_ref }}'] | [0]
            expect:
              fields:
                action: CREATE
                resource_ref: "{{ .vars.lookup_cluster_policy_ref }}"
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0
          - select: summary
            expect:
              fields:
                applied: 1
                failed: 0

  - name: 004-sync-idempotency-lookup-root-level-cluster-policy
    inputOverlayDirs:
      - overlays/005-add-root-level-lookup-policy
    commands:
      - name: 000-sync-no-changes
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 0
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0

  - name: 005-sync-omit-lookup-root-level-cluster-policy
    inputOverlayDirs:
      - overlays/006-remove-root-level-lookup-policy
    commands:
      - name: 000-sync-no-deletions
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 0
          - select: >-
              plan.changes[?resource_type=='event_gateway_virtual_cluster_cluster_policy' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0
      - name: 001-get-cluster-policies-after-lookup-omission
        run:
          - get
          - event-gateway
          - virtual-cluster
          - cluster-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "[?name=='{{ .vars.lookup_cluster_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.lookup_cluster_policy_name }}"
                description: "{{ .vars.lookup_cluster_policy_description }}"

  - name: 006-sync-with-declarative-cluster-policy
    inputOverlayDirs:
      - overlays/002-add-cluster-policy
    commands:
      - name: 000-sync-add-cluster-policy
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 1
                by_action.CREATE: 1
          - select: >-
              plan.changes[?resource_type=='event_gateway_virtual_cluster_cluster_policy' &&
              resource_ref=='{{ .vars.cluster_policy_ref }}'] | [0]
            expect:
              fields:
                action: CREATE
                resource_ref: "{{ .vars.cluster_policy_ref }}"
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0
          - select: summary
            expect:
              fields:
                applied: 1
                failed: 0

      - name: 001-get-cluster-policies
        run:
          - get
          - event-gateway
          - virtual-cluster
          - cluster-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "[?name=='{{ .vars.cluster_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.cluster_policy_name }}"
                description: "External sync cluster policy"

  - name: 007-sync-idempotency
    inputOverlayDirs:
      - overlays/002-add-cluster-policy
    commands:
      - name: 000-sync-no-changes
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 0

  - name: 008-sync-remove-declarative-cluster-policy
    commands:
      - name: 000-sync-no-deletions
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 0
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster_cluster_policy' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0

      - name: 001-get-cluster-policies-after-sync
        run:
          - get
          - event-gateway
          - virtual-cluster
          - cluster-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "[?name=='{{ .vars.cluster_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.cluster_policy_name }}"

  - name: 009-sync-with-declarative-produce-policy
    inputOverlayDirs:
      - overlays/003-add-produce-policy
    commands:
      - name: 000-sync-add-produce-policy
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 1
                by_action.CREATE: 1
          - select: >-
              plan.changes[?resource_type=='event_gateway_virtual_cluster_produce_policy' &&
              resource_ref=='{{ .vars.produce_policy_ref }}'] | [0]
            expect:
              fields:
                action: CREATE
                resource_ref: "{{ .vars.produce_policy_ref }}"
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0
          - select: summary
            expect:
              fields:
                applied: 1
                failed: 0

      - name: 001-get-produce-policies
        run:
          - get
          - event-gateway
          - virtual-cluster
          - produce-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "[?name=='{{ .vars.produce_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.produce_policy_name }}"
                description: "{{ .vars.produce_policy_description }}"

  - name: 010-sync-with-declarative-consume-policy
    inputOverlayDirs:
      - overlays/004-add-consume-policy
    commands:
      - name: 000-sync-add-consume-policy
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 1
                by_action.CREATE: 1
          - select: >-
              plan.changes[?resource_type=='event_gateway_virtual_cluster_consume_policy' &&
              resource_ref=='{{ .vars.consume_policy_ref }}'] | [0]
            expect:
              fields:
                action: CREATE
                resource_ref: "{{ .vars.consume_policy_ref }}"
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0
          - select: summary
            expect:
              fields:
                applied: 1
                failed: 0

      - name: 001-get-consume-policies
        run:
          - get
          - event-gateway
          - virtual-cluster
          - consume-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "[?name=='{{ .vars.consume_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.consume_policy_name }}"
                description: "{{ .vars.consume_policy_description }}"

  - name: 011-idempotency-all-declarative-policies
    inputOverlayDirs:
      - overlays/002-add-cluster-policy
      - overlays/003-add-produce-policy
      - overlays/004-add-consume-policy
    commands:
      - name: 000-sync-no-changes
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --dry-run
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 0
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0

  - name: 012-cleanup-declarative-policies
    commands:
      - name: 000-sync-no-deletions
        run:
          - sync
          - -f
          - "{{ .workdir }}/external-event-gateway.yaml"
          - --auto-approve
        assertions:
          - select: plan.summary
            expect:
              fields:
                total_changes: 0
          - select: plan.changes[?resource_type=='event_gateway_virtual_cluster' && action=='DELETE']
            expect:
              fields:
                "length(@)": 0

      - name: 001-get-cluster-policies-after-cleanup
        run:
          - get
          - event-gateway
          - virtual-cluster
          - cluster-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "length(@)"
            expect:
              fields:
                "@": 2
          - select: "[?name=='{{ .vars.cluster_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.cluster_policy_name }}"
                description: "External sync cluster policy"
          - select: "[?name=='{{ .vars.lookup_cluster_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.lookup_cluster_policy_name }}"
                description: "{{ .vars.lookup_cluster_policy_description }}"

      - name: 002-get-produce-policies-after-cleanup
        run:
          - get
          - event-gateway
          - virtual-cluster
          - produce-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "length(@)"
            expect:
              fields:
                "@": 1
          - select: "[?name=='{{ .vars.produce_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.produce_policy_name }}"
                description: "{{ .vars.produce_policy_description }}"

      - name: 003-get-consume-policies-after-cleanup
        run:
          - get
          - event-gateway
          - virtual-cluster
          - consume-policies
          - --gateway-name
          - "{{ .vars.egw_name }}"
          - --virtual-cluster-name
          - "{{ .vars.virtual_cluster_name }}"
          - -o
          - json
        assertions:
          - select: "length(@)"
            expect:
              fields:
                "@": 1
          - select: "[?name=='{{ .vars.consume_policy_name }}'] | [0]"
            expect:
              fields:
                name: "{{ .vars.consume_policy_name }}"
                description: "{{ .vars.consume_policy_description }}"
