_defaults:
  kongctl:
    namespace: org-sa-sync-e2e

apis:
  - ref: org-sa-sync-api
    name: org-sa-sync-api
    description: API used by the organization system account sync E2E scenario

organization:
  system-accounts:
    - ref: e2e-system-account
      id: "{{ .vars.system_account_id }}"
