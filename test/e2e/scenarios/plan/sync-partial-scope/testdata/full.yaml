_defaults:
  kongctl:
    namespace: sync-partial-scope

portals:
  - ref: sync-partial-portal
    name: sync-partial-portal
    display_name: "Sync Partial Portal"
    description: "Portal for sync partial scope test"
    authentication_enabled: false
    auto_approve_developers: false
    auto_approve_applications: false

apis:
  - ref: sync-partial-api
    name: sync-partial-api
    description: "API for sync partial scope test"
    labels:
      test: sync-partial-scope
