_defaults:
  kongctl:
    namespace: portal-email-config

portals:
  - ref: portal-email
    name: "portal-email"
    display_name: "Portal Email Config"
    kongctl:
      namespace: portal-email-config
