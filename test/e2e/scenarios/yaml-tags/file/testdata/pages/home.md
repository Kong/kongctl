# Portal 1 Home

This home page content is loaded using the `!file` tag to verify large text support.

- Headline and body come from an external Markdown file.
- Rendering confirms that nested portal resources can use file tags.
